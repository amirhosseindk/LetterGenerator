<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:LetterGenerator.Letter.Models;assembly=LetterGenerator.Letter"
             x:Class="LetterGenerator.MAUI.LettersListPage"
             Title="نامه‌ها"
             FlowDirection="RightToLeft">

	<VerticalStackLayout Padding="20" Spacing="20">
		<CollectionView x:Name="LettersList"
                        SelectionChanged="OnLetterSelected"
                        SelectionMode="Single">
			<CollectionView.ItemTemplate>
				<DataTemplate x:DataType="models:LetterDto">
					<Frame BorderColor="Gray" CornerRadius="5" Padding="10" Margin="5">
						<Label Text="{Binding Number}" FontSize="18"/>
					</Frame>
				</DataTemplate>
			</CollectionView.ItemTemplate>
		</CollectionView>

		<Button Text="🔙 بازگشت"
                FontSize="18"
                Clicked="OnBackClicked"
                HeightRequest="50"/>
	</VerticalStackLayout>

</ContentPage>